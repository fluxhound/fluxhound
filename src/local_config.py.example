"""Template for local test device configuration.

Copy this file to local_config.py (gitignored, never commit it) and
fill in the real device ID, IP address, and local key of your test
bulb, as found in CLAUDE.local.md.
"""

DEVICE_ID = "your-device-id"
IP_ADDRESS = "192.168.x.x"
LOCAL_KEY = "your-local-key"
PROTOCOL_VERSION = 3.3
